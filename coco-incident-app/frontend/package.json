{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Vite-based frontend for GAS web app",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "^5.3.3",
    "vite": "^7.2.4",
    "vite-plugin-singlefile": "^2.0.0"
  }
}
