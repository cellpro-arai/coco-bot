{
  "name": "backend",
  "version": "1.0.0",
  "description": "Google Apps Script backend",
  "scripts": {
    "build": "npm run remove-dist && vite build && npm run copy-manifest",
    "copy-manifest": "cp ../appsscript.json ../dist/",
    "remove-dist": "rm -rf ../dist",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/google-apps-script": "^2.0.8",
    "prettier": "^3.7.1",
    "typescript": "^5.9.3",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "date-fns": "^4.1.0"
  }
}
