{
  "name": "expenses-app",
  "version": "1.0.0",
  "description": "Google Apps Script order form with TypeScript",
  "scripts": {
    "build": "tsc && npm run copy-files",
    "copy-files": "cp src/index.html dist/ && cp appsscript.json dist/",
    "watch": "tsc --watch",
    "push": "npm run build && clasp push",
    "deploy": "npm run build && clasp push && clasp deploy"
  },
  "keywords": [
    "google-apps-script",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "@types/google-apps-script": "^1.0.83",
    "typescript": "^5.3.3"
  }
}